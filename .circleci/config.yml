version: 2.1

jobs:
  build:
    docker:
      - image: shenoyprathamesh123/py-node-mysql:0.0.1

    steps:
      - checkout

      - run:
          name: Check if python, node, and mysql are installed
          command: |
            pip --version
            node --version
            mysql --version
